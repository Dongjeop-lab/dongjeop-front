import { TERMS } from '@/lib/terms';

import TermsContentComponent from '../_components/terms-content';

const ProvacyPolicyTermsPage = () => {
  return <TermsContentComponent content={TERMS.PRIVACY_POLICY} />;
};

export default ProvacyPolicyTermsPage;
