import TermsContentComponent from '../_components/terms-content';
import { IMAGE_COLLECTION_TERMS } from '../_constants/image-collection';

const ImageCollectionTermsPage = () => {
  return <TermsContentComponent content={IMAGE_COLLECTION_TERMS} />;
};

export default ImageCollectionTermsPage;
