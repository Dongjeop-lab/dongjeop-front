# =============================================================================
# 환경변수 템플릿 - 카카오 클라우드 + nginx 배포용
# =============================================================================

# 🚀 사용 방법
# 
# 1단계: 이 파일을 복사해서 실제 환경변수 파일 만들기
#   터미널에서 실행: cp .env.example .env.local
#
# 2단계: .env.local 파일 열어서 [미정] 부분들을 실제 값으로 교체
#   예시: NEXT_PUBLIC_API_URL=[미정] → NEXT_PUBLIC_API_URL=http://localhost:3001
#
# 3단계: 파일 저장 후 개발 서버 실행
#   터미널에서 실행: npm run dev
#
# ⚠️ 주의사항:
# - .env.local은 절대 Git에 커밋하지 마세요 (개인 정보 포함)
# - .env.example은 템플릿이므로 실제 값을 넣지 마세요
# - [미정] 표시된 값들은 해당 서비스가 준비되면 설정하세요

# =============================================================================
# 🌐 클라이언트 접근 가능 환경변수 (NEXT_PUBLIC_ 접두사)
# 브라우저에서 접근 가능 - 민감하지 않은 정보만!
# =============================================================================

# 백엔드 API 기본 URL (필수)
# 📝 설정 방법: 백엔드 개발자가 서버 실행 후 알려주는 주소로 변경
# 📝 설정 시점: 백엔드 개발 시작 후
NEXT_PUBLIC_API_URL=[미정 - 백엔드 개발 후 설정]
# 개발환경 예시: http://localhost:3001
# 임시로 이렇게 설정: NEXT_PUBLIC_API_URL=http://localhost:3001

# 지도 API 키 (지도 기능 사용 시 필수)
# 📝 설정 방법: Google Maps 또는 네이버/카카오 지도 API 키 발급 후 입력
# 📝 설정 시점: 지도 기능 개발 시작 전
NEXT_PUBLIC_MAP_API_KEY=[미정 - 지도 서비스 결정 후 발급]
# 예시: AIzaSyBxxxxxxxxxxxxxxxxxxxxxxxxxxxxx (Google Maps)
# 임시로 비워두고 시작: NEXT_PUBLIC_MAP_API_KEY=

# 현재 도메인 (필수) - 이미 설정된 값
NEXT_PUBLIC_DOMAIN=localhost:3000
# 이 값은 개발환경에서 고정이므로 변경하지 마세요

# 환경 구분 (필수) - 이미 설정된 값
NODE_ENV=development
# 개발환경에서는 development로 고정

# =============================================================================
# 🔐 서버 전용 환경변수 (접두사 없음)
# 서버에서만 접근 가능 - 민감한 정보들
# =============================================================================

# 인증 및 보안 (당장 필요 없음 - 나중에 설정)
# JWT_SECRET=[미정 - JWT 시크릿 키 생성 후 설정]
# 📝 설정 시점: 로그인 기능 개발할 때
# NEXTAUTH_SECRET=[미정 - NextAuth.js 시크릿 생성 후 설정]
# 📝 설정 시점: NextAuth.js 사용할 때

# 외부 서비스 (당장 필요 없음 - 나중에 설정)
# REVIEW_SECRET_KEY=[미정 - 리뷰 업로드 보안 키 생성 후 설정]
# 📝 설정 시점: 파일 업로드 기능 개발할 때

# =============================================================================
# 📦 선택적 환경변수 (필요시에만 설정)
# =============================================================================

# CDN URL (외부 CDN 서비스 사용 시)
# NEXT_PUBLIC_CDN_URL=[미정 - CDN 서비스 설정 시 추가]

# 분석 및 모니터링
# NEXT_PUBLIC_ANALYTICS_ID=[미정 - Google Analytics 사용 시]
# NEXT_PUBLIC_SENTRY_DSN=[미정 - Sentry 에러 추적 사용 시]

# 지도 관련 (동접 프로젝트 특화)
# NEXT_PUBLIC_NAVER_MAP_CLIENT_ID=[미정 - 네이버 지도 사용 시]
# NEXT_PUBLIC_KAKAO_APP_KEY=[미정 - 카카오 지도 사용 시]

# 라벨링 이미지 저장 위치
# NEXT_PUBLIC_LABEL_IMAGE_BASE_URL = 
