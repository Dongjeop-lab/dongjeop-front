# =============================================================================
# 환경변수 템플릿
# 사용법: cp .env.example .env.local 후 실제 값으로 채우기
# =============================================================================

# =============================================================================
# 클라이언트 노출 환경변수 (NEXT_PUBLIC_ 접두사)
# 브라우저에서 접근 가능 - 민감하지 않은 정보만!
# =============================================================================

# 백엔드 API 기본 URL (필수)
NEXT_PUBLIC_API_URL=

# 지도 API 키 (지도 기능 사용 시 필수)
NEXT_PUBLIC_MAP_API_KEY=

# 현재 도메인 (필수)
NEXT_PUBLIC_DOMAIN=localhost:3000

# =============================================================================
# 🔐 서버 전용 환경변수
# 서버에서만 접근 가능 - 민감한 정보들
# =============================================================================

# 환경 구분 development | staging | production | test
NODE_ENV=development

# =============================================================================
# 📦 선택적 환경변수
# =============================================================================

# CDN URL (외부 CDN 서비스 사용 시)
# NEXT_PUBLIC_CDN_URL=

# 파일 업로드 시크릿 (업로드 기능 시)
# UPLOAD_SECRET_KEY=

# JWT 시크릿 (인증 직접 구현 시)
# JWT_SECRET=
